{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "10.5.1",
    "@sap/html5-repo-mock": "2.1.0"
  },
  "scripts": {
    "build": "mbt build",
    "build-countriesModule": "cd app && if [ ! -d ${workspaceFolder}/front-countries/app/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/front-countries/node_modules/react-scripts ]; then npm i; fi && npm run build",
    "start": "node ./approuter-start.js"
  },
  "dependencies": {
    "@sap/cds": "^6",
    "axios-oauth-client": "^2.0.2",
    "express": "^4",
    "react-app-rewired": "^2.2.1"
  }
}
